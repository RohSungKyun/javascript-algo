function solution(N, number) {
    var answer = 0;
    const use = Array.from(new Array(9), () => new Set());
    
    if(N==number) return 1;
    else {
        // 기능 구현
    }
    
    return answer;
}